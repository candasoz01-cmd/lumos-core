# Technical Debt Register

| Alan | Değer |
| --- | --- |
| Durum | Yürürlükte — 2026-07-20 |
| Kural | Borç üreten/fark eden PR buraya kayıt düşer ([Constitution §6](CONSTITUTION.md)); sessiz borç yok |
| Sıralama | Etkisi en yüksek üstte |

| ID | Borç | Etki | Çıkış kriteri | Durum |
| --- | --- | --- | --- | --- |
| TD-41 | ADR-031 üç parça (registry + capability + ledger) kernel + `lumos_gate_execute` opt-in; varsayılan kapalı. Approve-resume (`execute_approved_pending_record`) flag ON iken plan-bind + consume **wired**. Panel, CLI, `execute_tool_stub` / PC remote henüz `accept`/`consume` etmiyor (kalan **genişletme**). v1 ledger yerel JSONL hash-zinciri (WORM değil). Kabul crash kurtarma [TD-21](#td-21); ledger flock [TD-22](#td-22) | Env kapalıyken açık endpoint hâlâ grant'sız execute edebilir; kapı yalnız test ve opt-in gate/approve yolunda | Ürün kararıyla varsayılan-on **veya** kalan execute yüzeyleri + her yüzeyde unknown_task / unregistered_key / deviation / replay; CU4 yerine geçilmeden | Açık — bu PR bilinçli dilim · **Legacy ID:** TD-20 (çift kullanim; `#777` olayı TD-20'de kaldi, 2026-09-23 ayrıştırması) |
| TD-37 | **Kapalı prova metni için hiçbir süpürme mekanizması yok.** `rehearsal` politikasında kaynak/çeviri metni kayda yazılır ve **süresiz yaşar**. 24 saatlik pencerenin işletilmesi (süresi dolanı silme + zamanlayıcı) `73d6f58` (2026-09-11, *revert(devpost): restore submitted state except approved security fixes*) ile geri alındı: `ops/retention/` (installer + launchd şablonu) ve `tests/test_representative_retention.py` silindi; `require_sweeper` / `sweeper_status` / `SWEEP_INTERVAL_S` `main`'de yok (doğrulandı `d16eee04`). Kod bunu kendisi kabul ediyor — `src/representative/retention.py:24-27`: «24 saatlik metin penceresinin İŞLETİLMESİ … bu dilimde YOKTUR … `rehearsal` politikasında metin şu an süresiz yaşar.» **Legacy ID:** eski TD-29 (2026-09-04, `ed9ca2f`) — kayıt **yeniden yazıldı**: eski metin «süpürücü var ama makine kapalıyken koşmuyor» diyordu, bugünkü durum daha ağır | Sıfır-saklama (`real-meeting`) etkilenmez, orada metin hiç yazılmaz. Ama kapalı provada diskte kalan metnin ömrü tanımsız: «24 saati aşan metin yoktur» **denemez** ve mekanizma olmadığı için gizlilik iddiası ile gerçeklik arasındaki fark kapanmıyor. Runbook'taki sınır metni artık mekanizmasız bir sınır | Ya süpürme geri kurulur (açılışta/login'de tetiklenen süpürme + rig başlangıcında son süpürme yaşı kontrolü), ya da garanti verilmeyeceği ürün seviyesinde kabul edilip kullanıcıya söylenir. **Kabul kriteri (eski TD-30'un devraldığı madde):** süpürücü yeniden kurulursa launchd kurulum yolu uçtan uca doğrulanmadan bu kayıt kapanmaz — `launchctl bootstrap` gerçekten koşmalı; «betik ve şablon var» testi yetmez | Açık — **yüksek öncelik**, gizlilik yüzeyi (2026-09-23) |
| TD-21 | ADR-031 `accept_execution_task` önce boş `grant_id`/`token_hash` ile registry yazar, grant dosyası sonra gelir. İlk yazıdan sonra crash → retry `duplicate_task`; görev elle silinmeden yeniden mint edilemez. Availability, authz bypass değil | Kabul yarıda kalırsa görev kilitlenir; yeni anahtar basılamaz. Consume hâlâ kayıt/anahtar uyuşmazlığında red eder | Atomik kabul (tek yazı veya yarım kaydı retry'ın üzerine yazması) + crash-recovery testi; production enablement (TD-20) öncesi | **Kapandı** (#792): incomplete same-binding retry remints; `test_accept_retry_recovers_incomplete_registry` |
| TD-22 | ADR-031 `append_ledger_entry` kilit yok: tip oku → JSONL append → tip yaz. Eşzamanlı accept/consume/deny aynı `prev_hash`'i paylaşabilir, zinciri `verify_ledger_chain` reddeder. **Ledger enforcement gate değil** ([ADR-031](decisions/ADR-031-task-execution-grant.md); `test_ledger_is_evidence_not_the_gate`) — allow/deny değiştirmez | Denetim zinciri kırılabilir; yetki kararı değişmez. Kanıt sonradan doğrulanamaz | `append_ledger_entry` flock (veya eşdeğer) + eşzamanlı append testi; production enablement (TD-20) öncesi | **Kapandı** (#792): global ledger sidecar flock; `test_concurrent_ledger_appends_preserve_chain` |
| TD-01 | İki ayrı task store: panel `.lumos/tasks.json` vs TaskEngine `.lumos/tasks/tasks.json` | Görev sistemi (%60) büyüyemiyor; veri kayması riski; v0.5'in önündeki ana engel | Tek store sözleşmesi + göç + iki yüzeyin aynı kaynağı okuduğunu kanıtlayan test | **Kapandı** (PR #640, `31e7ccf`): canonical panel formatı + compatibility bridge; hedef 12/12, tam takım 1452 geçer. Geçici köprü → TD-11 |
| TD-11 | TD-01'in geçici köprüsü: `panel_bridge_state._read_tasks_payload` eski TaskEngine formatını (`task_id` + Türkçe statü) hâlâ okuyor (`task-store-v1.md` §Geçiş köprüsü) | Geçici köprünün kalıcılaşması yeni borç; iki-şema mantığı okuyucuda yaşamaya devam ederse TD-01'in kazanımı erir | Tüm canlı `tasks.json` dosyaları `migrate_engine_tasks` ile canonical'a geçirilip doğrulandıktan sonra: `_read_tasks_payload` legacy dalları + `_LEGACY_*_STATUSES` kaldırılır; sözleşmedeki kaldırma sürümü (v0.6) referans alınır | Açık — planlı |
| TD-02 | `ui/src/pages/panel.astro` ~4.6k satır tek dosya | Her panel değişikliği riskli ve yavaş; review edilemiyor | Bileşenlere bölme; dosya başına makul sınır; davranış değişmeden (read-only audit korunarak) | Açık |
| TD-03 | Monitoring bağlı değil: Sentry/Axiom kodu main'de, `SENTRY_DSN` / `LUMOS_AXIOM_TOKEN` prod'da yok | Canlı hatalar görünmez; geçmişe dönük log yok | İki env değişkeninin Vercel'e girilmesi (kullanıcı; ~5 dk) + ilk gerçek olayın Sentry'de görülmesi | Açık — kullanıcıda |
| TD-04 | Board katmanının gerçek kullanımı yok: sözleşme+projeksiyon+claim+gateway main'de ama hiçbir ajan yazmıyor/okumuyor | Bugünkü çarpışmalar (çifte KA-001, yabancı amend, PR kapatma) aynen tekrarlanabilir; KA-001 CANLI'ya çıkamıyor | En az bir gerçek çok-ajanlı oturumda claim + görünürlük kullanımının kayıtlı kanıtı | **Kapandı** (2026-07-20/21): gerçek çok-ajanlı kullanım kanıtlandı — `claude.code` claim'leri PANEL-136 (`d10924ca`, PR #637), TD-01-FOLLOWUP (`c1953bd3`, PR #642), TD-09 (`d07df521`, PR #644); `codex-root` claim'i KA-002-SECURITY-FOLLOWUP (PR #636). Claim listesi çakışan kapsamı görünür kıldı (TD-02 → Codex'te olduğu claim/dal üzerinden görüldü, girilmedi) |
| TD-05 | Çakışan draft PR'lar: #620 (21 dosya), #614 (9 dosya) main'in 55+ commit gerisinde | Rebase maliyeti her gün büyüyor; FAZ-3 kararına kadar çürüyor | Faz kararı: rebase sahibine atanır ya da kapatılır (iş ADR'de kalır) | **Kapandı** (2026-07-22): faz kararı **"üçüncü yol"** — rebase yok, seçici kurtarma. #620 → **#654** (`8563b5b`: platform CSS 654 satır, hero bileşeni, landing metinleri, visual-review kayıtları); #614 → **#655** (`171b840`: 4 showcase görseli + `presentation` metinleri, **0 silme**). İkisi de "partially recovered / superseded" gerekçesiyle kapatıldı. Taşınmayanlar: `index.astro` (main'de 8-9 commit'te yeniden yazıldı), `panel.astro` (TD-02'de), panel i18n rename (ChatLumos kararını geri alırdı). Kurtarma sırasında 3 gizli hata yakalandı: eksik `display: grid`, yanlış marka varlığı (tree → ChatLumos mark), bayat `heroAtaScene` bağlılığı |
| TD-13 | #620/#654'ten kurtarılan `lumos-platform.css` (655 satır) ve `LumosPlatformHero.astro` (66 satır) **bağlanmamış varlık**: hiçbir sayfa import etmiyor. İnceleme (2026-08-20, koddan doğrulandı) bunların planlı-ama-bağlanmamış değil, **bayatlamış bir kurtarma implementasyonu** olduğunu gösteriyor: `index.astro`'nun canlı hero'su aynı `hero.eyebrow`/`hero.pillar` anahtarlarını zaten taşıyor ve sonradan ask-band + `hero.audience` + `/auth` CTA'sıyla ilerledi; bileşenin kullandığı 14 `landing.platform.*` anahtarının hiçbiri i18n katalogunda yok (`i18n/runtime.ts` son çare olarak anahtarı ham metin basar); `lumos-platform.css`'in 32 sınıfından **24'ü** `index.astro` inline stiliyle çakışıyor; CTA'nın `#canli-durum` çapa hedefi repoda tanımlı değil | İki yönlü. Aceleyle silmek FAZ-3 ürün fikrini (5 katmanlı kontrol haritası anlatısı) kaybettirir. Buna karşılık kaydı "bağlanmaya hazır varlık" diye tutmak FAZ-3'e gerçekte var olmayan bir hazırlık borcu bırakır: bağlama işi 28 i18n girdisi + stil çatışmasının çözümü + sıfırdan canlı-durum bölümü demek ve sonucu canlı hero ile yarışan ikinci bir hero olur | FAZ-3 **ürün fikri korunur**; bu dosyaların gelecekte doğrudan entegre edileceği **varsayılmaz**. FAZ-3 açıldığında ihtiyaç, güncel hero + dashboard-health sözleşmesi üzerinde **yeniden uygulanır**; mevcut dosyalar o aşamada yeniden değerlendirilir veya silinir | Açık — **park** (2026-08-20 kararı): şimdi bağlama yok, şimdi silme yok |
| TD-14 | Toplantı-içi tercüme first-audio gecikmesi canlı Meet'te ölçülmedi; Cloud yalnız sentetik p50/p90. Eski prova medyan 3.49s / p90 5.79s (2026-08-14) yeni hedefi (p50≤2.5s p90≤4s) geçmiyor | Doğal sohbet ürün hedefi kanıtsız; PASS iddiası yasak | Mac canlı insan testi: speech-end→STT-final→translation-ready→TTS-start→first-audio-in-Meet jsonl; `first_audio_budget_pass` true + p50/p90 hedefleri | Açık — Mac prova |
| TD-23 | Meet ardıl tercümede konuşmacı diarizasyonu yok: giriş hâlâ `audio_mixed_raw` (karışık akış). 2026-08-24 kararı çıkışı tek sese indirdi ve VAD/cümle birleştirmeyi ekledi; konuşmacıları arkada ayırma bu dilimde yok | Üst üste binen iki konuşmacı tek VAD akışında birbirine karışabilir; çeviri kimin sözü olduğu belirsizleşir | Ayrı konuşmacı akışları veya diarizasyon + yine tek ortak çıkış sesi; canlı Meet prova kaydı | Açık — bilinçli erteleme (önce tek ses + yüksek bitiş eşiği) |
| TD-06 | Eski dallarda GIT_DIR sızıntılı test: 4b5cf19 öncesi dallarda pre-commit worktree'den koşunca repo config bozuluyor (2026-07-19'da yaşandı: `core.bare=true`, `user=Test`, artık dal) | Eski dalda çalışan her ajan repoyu bozabilir | Eski dala dokunmadan önce origin/main merge kuralı (Constitution §9 pratiği); #620/#614 rebase'lerinde zorunlu | Açık — kural yazıldı |
| TD-07 | Tarihi commit'lerde `Test <test@example.com>` kimliği (örn. #628 taban commiti `4d7bff2`; main'e merge edilmiş eski commit'ler) | Audit/attribution zinciri kirli | Merge edilmemiş dallarda amend (yalnız kullanıcı onayıyla); main tarihi YENIDEN YAZILMAZ, kabul edilmiş kayıp olarak not düşülür | Kısmen kapalı (#630/#628 HEAD düzeltildi) |
| TD-08 | Worktree kalabalığı: 25+ worktree, bir kısmı sahipsiz/bayat | Hangi dizinde kimin çalıştığı belirsiz; çarpışma yüzeyi | Sahipsiz worktree envanteri + kapanan PR'ların worktree'lerinin temizliği | **Kapandı** (2026-07-21): **30 → 16**. Merge olmuş + temiz + idle 14 worktree ve merged yerel dalları kaldırıldı (aktivite kontrolüyle, aktif oturuma dokunulmadan). Kalan 16: ana repo + açık PR/aktif dallar + gerçek uncommitted iş taşıyan 2 worktree (aşağıya bkz. TD-12) |
| TD-12 | İki worktree'de sahibi belirsiz uncommitted iş duruyor: `lumos-core-chat-identity` (api auth/session/hosted_lumos + **untracked `api/mobile/`**) ve `.codex-worktrees/internal-alpha-ux` (panel i18n TR/EN + test) | Commit edilmemiş iş kaybolabilir veya merge edilmiş panel i18n değişiklikleriyle çakışabilir (PR #637 aynı dosyalara dokundu) | Sahibi belirlenip ya PR'a dönüştürülür ya da bilinçli olarak atılır; karar kayda geçer | **Kapandı** (2026-07-21): iki yarı da kurtarıldı ve main'de. **A** → mobil OAuth deep-link akışı, PR #647 (`0478688`); riskleri `api-surface-v1.md` §Mobil akış'a yazıldı. **B** → 72 satır panel i18n sadeleştirmesi + render-anı demo testi, PR #651 (`cee3c0f`). Bayat taban nedeniyle **dosya kopyalanmadı**, doğrulanmış diff'ler taşındı; `internal-alpha-ux` worktree'si içeriğinin main'de olduğu kanıtlandıktan (diff artık uygulanamıyor) sonra kaldırıldı. Worktree **16 → 15** |
| TD-09 | `api/*` public yüzeyinin sözleşme dokümanı yok | Uçlar yalnız kodda tanımlı; iOS/3. taraf entegrasyonu körlemesine | `docs/contracts/` altında API yüzey sözleşmesi | **Kapandı**: `docs/contracts/api-surface-v1.md` (koddan doğrulanmış envanter; 9 uç, ortak kurallar, güvenlik sınırları, v1 sınırları) |
| TD-16 | Kimlik modeli ile kod arasındaki boşluk: [ADR-024](decisions/ADR-024-lumos-identity-multi-subject-model.md) beş özne (insan/Lumos/cihaz/agent/servis) kilitledi ama kodda yalnız cihaz kimliği gerçek (`src/security/identity.py`); `lumos_id_provider.py` stub (`real_identity_storage: False`), Representative'in **agent kimliği yok** (kurucunun oturumu içinde çalışıyor), **Lumos öznesinin hiç kod karşılığı yok** | Denetim kaydı bugün "kim, kim adına" sorusunu yapısal olarak cevaplayamıyor; agent yetkisi ile insan yetkisi ayrışmıyor. Model kâğıtta kilitli, kodda yok — "zaten var" sanılırsa üstüne yanlış kod yazılır | Uygulama kapısı kurucu tarafından açıldığında: özne kayıt şeması + `AuthorityGrant` (ADR-024 K4) + `actor`/`on_behalf_of`/`grant_id` üçlüsünün denetim kaydında kanıtlanması | Açık — **bilinçli**; uygulama FAZ-1/STOP LIST kararına bağlı, ADR-024 kod izni değildir |
| TD-10 | Vault claim deposu v1 güven sınırı: kimlikler self-asserted (bilinçli; `task-claim-v1.md` §Güven sınırı) | Kötü niyetli ajan sahte kimlik beyan edebilir | Gateway üzerinden kimlik doğrulamalı erişim (KA-003 devamı) — bilinçli v1 sınırı, unutulmasın diye kayıtlı | Bilinçli sınır |
| TD-15 | **Privacy/control gereksinimi (nice-to-have UI DEĞİL):** yerel mikrofon/ses yakalama (`local_rig --audio`, bot_rig STT) aktifken kullanıcıya görünür hiçbir gösterge yok ve süre sınırı yok. Canlı kanıt (2026-08-17): 2026-08-16 21:04'te başlatılan `local_rig --audio` **~20 saat** mikrofonu açık tutup sesi STT'ye akıttı; kurucu dahil kimse farkında değildi ("teste olduğumun farkında bile değilim"). Kural (kurucu, 2026-08-17): **Lumos aktif yerel yakalamayı kullanıcıdan gizleyemez** — görünür ve sürekli gösterge + unutulmuş capture için otomatik süre sınırı + güvenli kapanış | Gizlilik ihlali (habersiz kayıt), "Kontrol Sende" ilkesinin doğrudan ihlali, sessiz API maliyeti; güven kaybı riski ürün-kritik | Kullanıcı tek bakışta "mikrofon şu anda Lumos tarafından kullanılıyor" diyebilmeli; ne kadar süredir çalıştığını görebilmeli; tek hareketle durdurabilmeli; süre sınırı aşılırsa sistem kendisi güvenli kapanış yapmalı (transcript/jsonl flush dahil). Ek: SIGINT'in rig'i kapatmaması (bilinen) güvenli-kapanış kapsamında çözülür | Açık — öncelikli |
| TD-17 | Gerçek Meet sesi için Avrupa veri yerleşimi + MAM/ZDR yazılı org onayı henüz kayıtlı değil; batch STT yalnız sentetik/hassas olmayan test sesine açık | Canlı toplantı sesi OpenAI'ye gidemez; ürün vaadi (Meet tercüme) açılış kapısında durur | Organizasyonda yazılı MAM/ZDR + `eu.api.openai.com` (depolama ve işleme) doğrulaması kayda geçer; `LUMOS_STT_RESIDENCY_WRITTEN` ancak o kayıttan sonra set edilir. Sözleşme: [stt-data-boundary-v1](contracts/stt-data-boundary-v1.md) / [ADR-025](decisions/ADR-025-stt-openai-data-boundary.md) | Açık — **açılış kapısı** |
| TD-40 | Port çakışması: `representative/bot_rig.py` varsayılan portu 8765, Lumos runtime web uygulaması (`~/Library/Application Support/Lumos/runtime/web/app.py`) da aynı Mac'te 8765'i dinliyor (2026-08-17 tespiti; canlı insan testi 4 sırasında çakışma tesadüfen yaşanmadı, tünel öz-testi yakalardı) | Rig başlarken bind hatası ya da sessiz yanlış yönlendirme riski; tünel öz-testi fail-closed yakalar ama oturum hiç başlayamaz | `bot_rig` varsayılan portunun 8765 dışına taşınması (ör. 8766+) veya boş port otomatik seçimi; runbook komutları güncellenir | Açık · **Legacy ID:** TD-14 (çift kullanım; first-audio gecikmesi TD-14'te kaldı, 2026-09-23 ayrıştırması) |
| TD-18 | Cloudflared URL emission ≠ tunnel readiness. `cloudflared` public URL'yi stdout'a, DNS kaydı 1.1.1.1'de yayınlanmadan ~10 sn önce verebiliyor; bu aralıkta sistem resolver `gaierror`, fallback `dig` NXDOMAIN. `bot_rig.verify_tunnel()` 90 sn retry + fail-closed (bot yaratılmaz) doğru çalışıyor; IP+SNI fallback sağlam. **Patch yok** — `bot_rig.py` bozuk iş değil, bilinen transient altyapı. Kanıt: 2026-08-19 üç deneme, t+0s NXDOMAIN/EMPTY. Aktif masa değil (STT/MAM-ZDR kapısı duruyor) | Nadiren 90 sn pencere boyunca DNS yeterince kararlı olmayınca öz-test fail-closed kapanır; toplantıya sağır bot sokulmaz | Reliability iyileştirmesi yapılırsa readiness URL parsing aşamasında değil, DNS + uçtan uca WS probe başarısıyla tanımlanmalı; fail-closed semantiği korunur | Açık — park |
| TD-19 | Panel sağlık rozetleri ölçüme dayanmıyor. [dashboard-health-v1](contracts/dashboard-health-v1.md) kilitli. **Tek kart** `bridge.llm` observe-slice `#772` (`9abf58de`) ile `main`'de; mikro-sorumluluk **`bridge.llm → Observe`** kayıtlı ([`src/dashboard_health/responsibility.json`](../src/dashboard_health/responsibility.json)). **Diğer 17 rozet hâlâ statik markup.** Meta connections ucu bağlanmadı. | Kullanıcı panelde çoğunlukla ölçüme dayanmayan sağlık iddiası görüyor. Observe grant 17 kartı kapsamaz; Fix/Escalate/alan sahipliği yok | [ADR-029](decisions/ADR-029-dashboard-health-earned-responsibility.md): izle atomu verildi; düzeltme/yükseltme/alan devri ayrı ve kapalı. TD-13/TD-14/vitrin bu çıkışa karışmaz | Açık — 17 rozet statik; **alan devredilmedi**; Observe yalnız `bridge.llm` |
| TD-25 | ADR-033 Account Activity Correlation çekirdeği yerel ve opt-in; canlı cihaz yayını, yapılandırılmış uyarı ingest’i ve panel Denetim merceği yok. `attestation_ref` opak özet; donanım doğrulaması yok. Mail ürünü kapalı (v2). | Kullanıcı “bu giriş bana mı ait?” diye sorduğunda çekirdek testtedir, canlı kanıt yolu yoktur | Onaylı cihaz istemcisinin minimal oturum özeti + yapılandırılmış uyarı ingest’i + Denetim merceği açıklaması (yeni sayfa yok); CANLI denmeden önce | Açık — bilinçli v0.1 sınırı · **Kimlik notu:** `TD-25` numarası 2026-09-04–09-11 arasında başka bir borcu (çevrimdışı replay ayrışması) işaret ediyordu; o kayıt artık **TD-33** |
| TD-20 | Standing-excluded PR bot ile açıksız merge. `candasoz01-cmd/lumos-core#777` · MERGED · head `c50127b6` · merge `339840f2` · `cursor[bot]` · GitHub API · 2026-08-20T14:32:29Z. PR gövdesi ve sözleşmenin kendi satırı **ADR-028 standing hattı yok / insan onayı zorunlu** diyordu; diff `docs/contracts/dashboard-health-v1.md` + ADR-029. Teknik kapılar yeşildi; eksik olan check değil **otorite onayı**. İçerik geri alınmaz (invariant 7 `main`'de). | Hariç sınıf kâğıtta; merge fiilen standing hattından geçebilir. Bir sonraki çekirdek sözleşme PR'ı aynı delikten inebilir | Sınıflandırıcı fail-closed: hariç yol (önek/dosya/jeton) **veya listelenmeyen yol** → `standing_merge=false`. Yalnız `docs/` / `tests/` allowlist ve hariç kuralına çarpmayan yol `eligible`. `#777` ve `src/security/` excluded. CheckRun `standing-class` hariçte fail. Bu CheckRun GitHub required check **yapılmaz**. Fiziksel kilit ayrı `merge-authority` modeli | Açık — sınıflandırıcı `#789`; fiziksel GitHub kilidi değil |
| TD-24 | `panel/scripts/panel_tasks_server.py` kimlik doğrulaması olmadan mutasyon kabul ediyor ve `Access-Control-Allow-Origin: *` yayınlıyor (satır 770). Dosyada `Authorization` veya `Origin` kontrolü **hiç geçmiyor** (`origin/main` `80b028a` üzerinde doğrulandı). Sunucu loopback'e bind oluyor (`127.0.0.1`, `:1611-1612`) ama loopback tek başına tarayıcıyı durdurmaz: kullanıcının tarayıcısında açık **herhangi bir sayfa** `127.0.0.1:8766/tasks`'e doğrudan `POST` atıp WebMCP tool katmanını, consent kapısını ve onay modalini tamamen atlayabilir | Panelin bütün insan-onayı mimarisi tek bir HTTP çağrısıyla devre dışı kalabilir. Bugün koruyan tek şey varsayılan fail-closed policy default'ları (`LUMOS_MODE=offline` → write deny; `LUMOS_PROFILE=rapor` → `safe_local` yok) — yani koruma niyetli bir kapı değil, konfigürasyon tesadüfü. `guvenli_yurut`+`online` ile çalışan bir kurulumda kapı tamamen açık | **Faz-1 (bu dilim, kapandı):** `Origin` allowlist — yalnız `http://127.0.0.1:*`, `http://localhost:*`, `http://[::1]:*`; `Access-Control-Allow-Origin: *` kaldırıldı, izinli origin yankılanır (`Vary: Origin`). Kapı `do_GET`/`do_PUT`/`do_POST` **ve** `do_OPTIONS` preflight'ında. **Faz-2 (bu dilim, `#830`):** Origin sonrası zorunlu `LUMOS_PANEL_TASKS_SECRET`; `X-Kando-Token` / `Authorization: Bearer`; boş sır `401 missing_secret`. İlk HTTP dilimi paylaşılan sır (E2E `window.LUMOS_PANEL_TASKS_TOKEN`). PKCE store kütüphanede; `/lumos-panel-auth/exchange` yok. Statik `/` `/index.html` `/js/` `/css/` kimliksiz; `OPTIONS` yalnız Origin | **Faz-1 kapandı** (2026-09-04): `tests/test_panel_tasks_origin_allowlist.py` — 23 koşum; yabancı origin'den `POST /tasks` → 403 ve **hiçbir yazma olmadığı** doğrulanıyor, preflight de reddediliyor, `*` regresyon kilidiyle kapalı. **Kapsam sınırı — açıkça:** bu koruma **tarayıcı-origin** korumasıdır. `Origin` başlığını tarayıcı koyar; başlık göndermeyen (`curl`, node, CLI) istekler bilinçli olarak **geçirilir** — yoksa E2E/CLI akışı kırılırdı. Makinede kod çalıştırabilen kötü niyetli bir süreç ve `Origin`'i **uyduran** bir istemci **kapsam dışıdır**; onları Faz-2 durdurur. Faz-1: candasoz01-cmd/lumos-core#829 · MERGED · b6a1eaa57b72e0e2753480b4bdb6fc5233ccf182 · GitHub API · 2026-09-04T13:13Z. Faz-2 HTTP bağlama `#830` (bu dal); cookie/PKCE HTTP ucu hâlâ açık |
| TD-26 | F6: `run_lumos_gate` `classify_risk==unknown` iken `{status: approved}` dönüyordu; pending/onay ve audit yoktu. `_risk_gate_execution_mode("unknown")` restricted, `dispatch_task` `needs_review` — gate fail-open. | Sınıflandırılamayan görev HTTP 200 boş başarı; Onaylar kartı açılmaz; iz yok | Unknown → `pending_approval` + `lumos_audit_log.reason=unknown_risk`; `validate_pending_for_approval` high\|unknown; test: `tests/test_lumos_gate_unknown_risk.py` | **Kapandı** (bu PR): fail-closed pending + zorunlu audit · **Kimlik notu:** `TD-26` numarası 2026-09-04–09-11 arasında başka bir borcu (WebMCP ajan yetki kapsamı) işaret ediyordu; o kayıt artık **TD-34** |
| TD-33 | Çevrimdışı panel mutasyonu replay'de reddedilirse yerel kayıt kullanıcıda kalıyor ve **kullanıcı uyarılmıyor**. `#gorevler-sync-badge` yalnız `shouldSkipGorevlerTasksApi()` ile sürülüyor (`ui/src/components/panel/PanelRuntime.astro:1752-1756`) — reddedilen replay ile ilgisi yok; kuyruk flush hatası `.catch(() => {})` ile yutuluyor (`:3335`). Sınıf: tutarlılık / görünürlük — **yetki aşımı DEĞİL**, policy replay'de gerçekten çalışıyor. **Legacy ID:** eski TD-25 (2026-09-04, `ed9ca2f`; `73d6f58` ile registry'den düştü) | Kullanıcı, sunucunun reddettiği bir görevi kendi panelinde var sanmaya devam ediyor; iki kaynak ayrışıyor ve ayrışma hiçbir yüzeyde görünmüyor | Reddedilen replay kullanıcıya görünür olmalı: ya geri alınır ya «sunucu reddetti» durumuyla işaretlenir; `MAX_ATTEMPTS` tükenince ne olduğu tanımlanır | Açık — `d16eee04`'te doğrulandı (2026-09-23) |
| TD-34 | WebMCP tool çağrılarında ajan bazlı yetki kapsamı yok: `ui/src/components/panel/WebMcpTools.astro` şemalarında `scope` / `agent_id` / `subject_id` alanı **yok** (doğrulandı `d16eee04`: sıfır eşleşme; tek `permission` eşleşmesi şema alanı değil açıklama metni). **Legacy ID:** eski TD-26 (2026-09-04, `ed9ca2f`; `73d6f58` ile düştü) | Tüm ajanlar tek ve aynı yetkiye sahip; «daha az yetkili ajan» ifade edilemiyor. Yeni bir tool'u yalnız belirli ajanlara açmanın yolu yok. TD-16 ve TD-10 ile aynı kökten | Tool çağrısı çağıranın kimliğini ve talep ettiği kapsamı taşır; sunucuda talep edilen kapsam ⊄ verilen kapsam ise reddedilir. TD-16'nın özne şemasına bağlı — ondan önce yapılırsa ikinci bir kimlik modeli doğar | Açık — TD-16'ya bağımlı |
| TD-35 | `lumos-list-tasks` Tasks API'ye hiç gitmiyor; panelin bellekteki `panelGorevlerTasks` projeksiyonunu döndürüyor (doğrulandı `d16eee04`: `local_projection` → 0 eşleşme, liste hâlâ projeksiyonu okuyor). Yazma yolları (propose/complete) API-backed; yalnız okuma yolu değil. **Legacy ID:** eski TD-27 (2026-09-04, `ed9ca2f`; `73d6f58` ile düştü) | Okuma yolu sunucu policy/confirmation kapılarından geçmiyor ve tek doğruluk kaynağı yok: TD-33'teki ayrışma durumunda ajan, sunucunun bilmediği görevleri okur. Ayrışma senaryosu hiç test edilmedi | `lumos-list-tasks` ya Tasks API'den okur (okuma da sunucu kapısından geçer), ya da döndürdüğü verinin yerel projeksiyon olduğunu ajana açıkça bildirir; ayrışma senaryosu için test. Hazır dilim: `candasoz01-cmd/lumos-core#835` (draft) | Açık — `#835` bu kimliğe bağlanır |
| TD-36 | Tool şemalarında yıkıcılık / salt-okunurluk beyanı yok: `WebMcpTools.astro` içinde `classification` / `readOnlyHint` / `destructiveHint` / `annotations` / `toolClassOk` → **sıfır eşleşme** (`d16eee04`). `SECURITY_NEVER_AUTO` ve kalıcı silme kapısı gerçek ve fail-closed, ama tool yüzeyine **bağlı değil** — bugünkü koruma «yıkıcı tool yazmadık» varsayımına dayanıyor. **Legacy ID:** eski TD-28 (2026-09-04, `ed9ca2f`; `73d6f58` ile düştü) | Yeni bir write tool eklendiğinde onu `SECURITY_NEVER_AUTO` eşlemesine bağlamayı hatırlatan mekanizma yok; koruma insan dikkatine bağlı. Yüzey büyüdükçe risk büyür | Her tool şemasında zorunlu sınıflandırma alanı (read-only / mutating / destructive) + sınıflandırması olmayan tool'un kayıt olmasını engelleyen test; destructive sınıfın `SECURITY_NEVER_AUTO` ile bağlanması. Hazır dilim: `candasoz01-cmd/lumos-core#834` (draft) | Açık — `#834` bu kimliğe bağlanır |
| TD-38 | **Merge blocker — ADR numara çakışması (`#807`).** Açık draft `candasoz01-cmd/lumos-core#807` (`cursor/security-completion-min-fixes`) `docs/decisions/ADR-025-agent-wall-visibility-slice.md` ekliyor; `main`'de **zaten** `docs/decisions/ADR-025-stt-openai-data-boundary.md` var. Dal 2026-09-23 itibarıyla hâlâ iki ADR-025 dosyası taşıyor. **Legacy ID:** eski TD-31 (2026-09-04, `ed9ca2f`; `73d6f58` ile düştü — yani blokajın gerekçesi bir süre canonical kayıtta yoktu) | Merge olursa «ADR-025» yazan her referans hangi belgeyi kastettiğini söyleyemez. `src/standing_merge/excluded_paths.json` önek eşlemesi ikinci dosyaya birincinin sınıflandırma kuralını miras ettirir; bugün etkisi güvenli yönde (ikisi de `excluded` üretir) ama sessiz bir yanlış eşleşme | `main`'deki ADR-025 **yeniden numaralanmaz** (referanslar ve `excluded_paths.json` kuralı ona bağlı); `#807`'deki yeni dosya boş bir numara alır. Tahsis mekanizması: TD-39 | Açık — **merge blocker**, `#807` review'a girmeden düzeltilmeli |
| TD-39 | **ADR ve TD numara tahsisi kayıtsız; çakışmalar artık `main`'e inmiş durumda.** `docs/decisions/` altında iki numara çift: `ADR-021-meta-multi-connection-model.md` + `ADR-021-robotics-sovereignty-layer-v0-1.md`; `ADR-022-meta-write-authority-domains.md` + `ADR-022-plan-a-dilimi-sandbox-inbound-reply.md`. Borç tarafında aynı desen: `TD-14` ve `TD-20` çift kullanılmıştı — bu reconciliation'da **TD-40** ve **TD-41** olarak ayrıştırıldı. **Bu kayıt eski TD-32'nin (2026-09-04, `#823` çakışması) yerine geçer; o olay kapandı ve geri getirilmez** — ama teşhisi doğru çıktı: «tahsis kayıtsız, üçüncüsü de gelir» | İki farklı mimari karar aynı numarayla anılıyor; ADR referansları belirsizleşiyor. Bu sefer çakışma dalda değil, `main`'e merge olmuş durumda — düzeltmesi referans güncellemesi gerektiriyor | (1) `docs/decisions/` ve bu registry için numara benzersizliğini doğrulayan test, CI'da koşar; (2) yeni ADR/TD açan PR numarayı tek bir tahsis kaydından alır; (3) `main`'deki ADR-021 / ADR-022 çiftlerinin ayrıştırılması — ayrı iş | Açık — 2026-09-23 |

## Kimlik geçmişi — kullanımdan kaldırılan TD numaraları

`ed9ca2f` (2026-09-04) ile `73d6f58` (2026-09-11) arasında bu registry TD-01…TD-32
içeriyordu. `73d6f58` — *revert(devpost): restore submitted state except approved
security fixes* — TD-25…TD-32 satırlarını düşürdü; ardından **TD-25 ve TD-26
numaraları başka konulara yeniden atandı**. Bu, aynı kimliğin iki farklı borcu
işaret etmesine yol açtı.

2026-09-23 reconciliation'ı (`candasoz01-cmd/lumos-core#882`):

| Tarihsel kimlik | Bugünkü karşılığı |
| --- | --- |
| eski TD-25 | **TD-33** |
| eski TD-26 | **TD-34** |
| eski TD-27 | **TD-35** |
| eski TD-28 | **TD-36** |
| eski TD-29 | **TD-37** — yeniden yazıldı; bugünkü durum eski kayıttan ağır |
| eski TD-30 | Ayrı kayıt değil — **TD-37**'nin kabul kriteri |
| eski TD-31 | **TD-38** |
| eski TD-32 | Geri getirilmedi; yerine **TD-39** (numara tahsis mekanizması) |

**TD-25…TD-32 tarihsel kimlikleri kullanımdan kaldırılmıştır** ve yeni kayıtlara
verilmez. Bugünkü TD-25 ve TD-26 kayıtları geçerlidir; ilgili satırlarda kimlik
notu taşırlar. Yeni canonical kayıtlar TD-33'ten başlar; sıradaki boş numara **TD-42**.

Çift kullanım ayrıştırması: `TD-14` first-audio gecikmesi kaydında kaldı, port
çakışması **TD-40** oldu; `TD-20` `#777` standing ihlali kaydında kaldı (AGENTS.md,
CONTRIBUTING.md, `standing-class.yml` ve ADR-028 buna atıf yapıyor), ADR-031
varsayılan-kapalı kaydı **TD-41** oldu.

Hiçbir kayıt silinmedi; her taşınan satır `Legacy ID` notu taşır.
