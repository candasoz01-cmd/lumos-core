# Scripts taşındı

Bu dizin artık kullanılmıyor. Yardımcı scriptler `lumos_core.scripts` altında:

- `python -m lumos_core.scripts.init_keystore`
- `python -m lumos_core.scripts.init_identity`

Eski dosyalar (init_keystore.py, init_identity.py) silindi; tek kaynak: `src/lumos_core/scripts/`.
