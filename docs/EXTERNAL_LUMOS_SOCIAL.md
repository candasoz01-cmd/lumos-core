# Dış lumos-social klasörü (repo dışı / arşiv)

**Konum:** `~/WORK_2026/lumos-social`

- Bu klasör **repo değildir** (lumos-core’un parçası değil). Kendi `.git`’i vardır; eskiden ayrı geliştirme/sandbox olarak kullanıldı.
- **Kaynak gerçek:** Monorepo içindeki `lumos-core/lumos-social/` dizinidir. Bu dış klasöre taşıma veya senkron zorunlu değildir.
- **Silinmez.** İstersen yerel denemeler veya arşiv olarak bırakılabilir. Repo’ya bağlanmaz; CI/test/kurulum bu klasörü kullanmaz.

Özet: **Repo dışı / arşiv.** Karışıklığı önlemek için geliştirme yaparken monorepo içindeki `lumos-social/` kullanılır.
