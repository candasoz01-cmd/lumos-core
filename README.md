# WORK_2026

**Bu klasör git repo değildir ve olmayacak.** Tek kaynak repo: **`lumos-core`**.

- Tüm git işlemleri: `cd lumos-core` (veya `lumos` alias). Bu dizinde (`WORK_2026`) `git` çalıştırma.
- `lumos-social`, `lumos-quantum`: sadece kardeş klasörler; lumos-core repo’sunun dışında. Kendi repoları varsa bağımsız repolar, yoksa sadece klasör. lumos-core içinde gömülü kopya yok.
