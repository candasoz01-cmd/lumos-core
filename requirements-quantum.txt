# Optional quantum stack for local Aer connect spike (not installed in default CI).
# Install: pip install -r requirements-quantum.txt
# Or: pip install 'lumos-core[quantum]'
qiskit>=1.0
qiskit-aer>=0.14
